
<%= link_to "add post", new_board_post_path(@board) %>

<h1>title:</h1>
<div class="field">
  <h2><%= @board.title %></h2>
</div>
<div class="field">
  <h2><%= @board.intro %></h2>k
</div>

<%= link_to "首頁", boards_path %>
